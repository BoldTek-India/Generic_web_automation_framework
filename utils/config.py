class Config:
    WEB_APP_NAME = "crm"  # Specify the web app to test
    BROWSER = "chrome"  # Browser type
    BASE_URL = "https://dev-crm.boldtek.com/app/auth/login"  # Base URL of the web app
    IMPLICIT_WAIT = 10  # Implicit wait time in seconds
